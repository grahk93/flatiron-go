<%= form_for @meetup, :url => url_for(:controller => 'meetups', :action => 'invite') do |f| %>
  <% @noninvitees.each do |noninvitee| %>
    <%= check_box_tag 'invitees[]', noninvitee.id %>
    <%= f.label :invitees, noninvitee.user_name %><br>
  <% end %>
  <%= f.submit %>
<% end %>