<!--<%= form_for @user do |f| %>
  
  <% if @user.errors.any? %>
    <h3>Errors:</h3>
      <% @user.errors.keys.each { |attr| @user.errors[attr].delete_at(1) } %> 
        <ul>
        <% @user.errors.full_messages.each do |msg| %>
          <li><%= msg %></li><br>
        <% end %>
        </ul>
  <% end %>

  <%= f.label :cohort %>
  <%= f.collection_select :cohort_id, Cohort.all, :id, :name %><br>
  <%= f.label :email %> 
  <%= f.text_field :email %><br>
  <%= f.label :user_name %> 
  <%= f.text_field :user_name %><br>
  <%= f.label :password %> 
  <%= f.password_field :password %><br>
  <%= f.submit "Sign Up" %> 

<%end%> -->


<section id="login" class="pad-lg light-gray-bg">
  <div class="container">
    <div class="row">
      <div class="col-sm-8 col-sm-offset-2 text-center">
        <br/>
        <br/>
        <span class= "glyphicon glyphicon-user aria-hidden="true"> </span>
        <h2 class="black">Sign Up</h2>
        <br/>

          <div class="row">
            <div class="col-sm-8 col-sm-offset-2 col-md-6 col-md-offset-3">
              <%= form_for @user, :class => "form" do |f| %> 
  
  <% if @user.errors.any? %>
    <h3>Errors:</h3>
      <% @user.errors.keys.each { |attr| @user.errors[attr].delete_at(1) } %> 
        <ul>
        <% @user.errors.full_messages.each do |msg| %>
          <li><%= msg %></li><br>
        <% end %>
        </ul>
  <% end %>

                  <div class="form-group">
                    <%= f.collection_select :cohort_id, Cohort.all, :id, :name, {:include_blank => "Please select your cohort"}, {:class => 'form-control'} %>
                    <br/>
                    <%= f.text_field :email, :class => 'form-control', :placeholder => 'Flatiron School Email' %>
                    <br/>
                    <%= f.text_field :user_name, :class => 'form-control', :placeholder => 'Username' %>
                    <br/>
                    <%= f.password_field :password, :class => 'form-control', :placeholder => 'Password' %>
                    <br/>
                  </div>
                  <%= f.submit "Sign Up", :class => "btn btn-primary btn-lg" %>
                <% end %>
              </div>
            </div><!--End Form row-->
            <br/>
            <p> Already registered?  <b><%= link_to "Log in!", login_path %> </b></p>
          </div>
        </div>
      </div>
    </section>